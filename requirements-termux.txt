# Termux-optimized requirements with compatible versions
aiogram==3.4.1
yt-dlp>=2024.9.27
python-dotenv==1.0.0
requests>=2.31.0
# Use older pydantic that doesn't require Rust compilation
pydantic==1.10.12
# Install aiohttp and other dependencies separately to avoid conflicts
aiohttp>=3.8.0
aiofiles>=0.8.0
